from plyer import notification

title = "GOD ALL ABOVE EVERYTHING"

message = "The Inner Mukonge Eric (TIME)! Am truly blessed!"

notification.notify(title = title,
                    message = message,
                    app_icon = None,
                    timeout = 10,
                    toast = False)